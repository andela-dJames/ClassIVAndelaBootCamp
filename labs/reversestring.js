function reverseString(str) {
  var output = str.split("").reverse().join(''); 
    if (str.length === 0){
      return null;
    }
     else if (str === isNaN) {
      return true;
    }
     else if (str === output) {
      return true;
    }
      else {
      return output;
    }   
}
